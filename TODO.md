# TODO

- Add `.staff` import/export affordances in the UI.
- Add parser snapshot fixtures for the bundled StaffScript examples.
- Add a script to regenerate `song-idea-01/score.json` and `score.musicxml` from source.
- Improve MusicXML articulation output for `staccato`, `tenuto`, and similar expressions.
- Decide whether future section blocks are always inline content, reusable definitions, or both.
- Expand chord mode only after documenting the supported harmony subset.
